describe('Berlin Clock', () => {
    it("should return 1 for seconds pair", () => {
        expect(1).toBe('1');
    });
    it("should return 0 for seconds odd", () => {
        expect(1).toBe('1');
    });
});